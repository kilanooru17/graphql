type Book {
    id: ID!
    title: String!
    author: String!
}

type Query {
    allBooks: [Book!]!
    bookById(id: ID!): Book
}

type Mutation {
    addBook(title: String!, author: String!): Book!
}
